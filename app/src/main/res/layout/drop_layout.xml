<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/drop_message"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/like_button"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:backgroundTint="@color/colorTransparent"
        android:contentDescription="@string/like_btn_desc"
        android:cropToPadding="false"
        android:scaleType="fitXY"
        android:src="@drawable/like_transparent"
        app:layout_constraintStart_toStartOf="@+id/drop_message"
        app:layout_constraintTop_toBottomOf="@+id/drop_message" />

    <TextView
        android:id="@+id/like_total"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="@color/colorOnPrimary"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@+id/like_button"
        app:layout_constraintStart_toStartOf="@+id/like_button"
        app:layout_constraintTop_toBottomOf="@+id/like_button" />

    <ImageButton
        android:id="@+id/report_button"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:backgroundTint="@color/colorTransparent"
        android:contentDescription="@string/report_btn_desc"
        android:scaleType="fitXY"
        android:src="@drawable/report_transparent"
        app:layout_constraintEnd_toEndOf="@+id/drop_message"
        app:layout_constraintTop_toBottomOf="@+id/drop_message" />

    <TextView
        android:id="@+id/report_total"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="@color/colorOnPrimary"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@+id/report_button"
        app:layout_constraintStart_toStartOf="@+id/report_button"
        app:layout_constraintTop_toBottomOf="@+id/report_button" />

</androidx.constraintlayout.widget.ConstraintLayout>