query DroppedAround($latitude: Float!, $longitude: Float!, $radius: Float!) {
  droppedAround(latitude: $latitude, longitude: $longitude, radius: $radius) {
    id
    text
    color
    location {
      latitude
      longitude
      altitude
    }
    likeState
    likeCount
    dislikeCount
  }
}